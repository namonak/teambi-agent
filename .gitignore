
# Claude Code runtime
.claude/db/context.db
.claude/.ctx_state*
.claude/.backup-*
.claude/.ralph_state*
.claude/.hud_cache*
.claude/.cost_state*
.claude/.hook_feedback*
.claude/.project_root*
.claude/.messenger_enabled*

# Node
node_modules/
.env
# 이미지 빌드 시 Dockerfile이 생성 (커밋 대상 아님)
build-info.json
npm-debug.log*
.DS_Store

# Caddy 실사용 설정 (도메인 포함 — .example만 커밋)
deploy/Caddyfile
