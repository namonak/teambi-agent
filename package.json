{
  "name": "teambi-agent",
  "version": "0.1.0",
  "description": "장부장 — Teams 단체 채팅의 카드 승인 문자·자연어 메시지로 팀비 지출을 자동 기입하는 Bot 앱",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@microsoft/agents-hosting": "^1.8.1",
    "express": "^4.21.2",
    "openai": "^6.48.0"
  }
}
