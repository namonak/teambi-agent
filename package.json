{
  "name": "teambi-agent",
  "version": "0.1.0",
  "description": "장부장 — Teams 채널의 카드 승인 문자·자연어 메시지로 팀비 지출을 자동 기입하는 AI Agent",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "express": "^4.21.2"
  }
}
